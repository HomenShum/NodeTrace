# Entity inspection evidence

Start with the repository HANDOFF.md for developer setup. This packet records a reviewer choosing an actor, tool, artifact or step and checking the exact events behind it. The native selector retains complete names when crowded canvas labels shorten or hide. Selected details and visits use the same reconciled graph as the canvas; metadata changes retain unchanged type filters and existing optional-visit and edge reconciliation behavior.

| Recorded proof | Current observation | Local artifact |
| --- | --- | --- |
| Source UI, seven widths | 3368 checks, 128 captures, 16 source bindings | [Source receipt](E6c-nodetrace-entity-canonical-source/receipt.json) |
| Installed Next, seven widths | 1855 checks, 95 captures, rebuilt production-mode local server | [Next owner report](E6c_NODETRACE_INSTALLED_NEXT_ENTITY_OWNER_PROOF.json) |
| Installed Vite, seven widths | 1874 checks, 95 captures, documented development route | [Vite owner report](E6c_NODETRACE_INSTALLED_VITE_ENTITY_OWNER_PROOF.json) |
| Independent ownership knockout | 127 checks including selection, metadata, omitted visits and reconciled relationships | [Owner report](E6c_NODETRACE_ENTITY_OWNER_KNOCKOUT.json) |
| Exact matched before/after | Original six-event source and four-event installed inputs; 6 completed cells, 12 captures, 36 checked PNGs | [Matched report](E6c_NODETRACE_MATCHED_CANONICAL_VISUAL.json) |

The installed owner proofs explicitly update one renderer file in normally installed consumers, then execute ordinary builds and their displayed refresh command. Earlier complete installation proof is retained separately. Vite's optional demo HTML remains a development route; its ordinary host build does not include that optional page.

## Inspect the actual comparison

- Original 390-pixel source boundary: [before](evidence/nodetrace-entity-inspection-02-before/primary-controls-boundary/source-390-populated-change-boundary.png).
- Current source selection and details: [after](E6c-nodetrace-matched-canonical-visual/source-390-native-selected-change-boundary.png).
- Current installed desktop selection: [after](E6c-nodetrace-matched-canonical-visual-retry-02/installed-1440-native-selected-change-boundary.png).

The matched input is replayed byte-for-byte through browser routing. It is not newly generated backend output. Each final PNG is exactly the byte buffer independently counted by the PIL raster check, with more than 40 dark pixels inside the canvas. Attempts are capped at eight and retained. The current-frame recorder independently rejects stale glyphs, out-of-bounds labels and collisions.

The first matched run completed five cells, then its final desktop overview had no current recorded glyphs. The diagnostic retry rendered seven labels but a probe encoding mistake corrupted the expected punctuation. Both failures remain. The successful retry uses the original UTF-8 script and the same frozen source, installed build and fixture hashes. The six successful cells are combined with that explicit history; the failed full run is never relabeled passed.

## Reproduce and interpret

Use Node 22. From the repository run `npm ci`, `npm run check`, then `node scripts/ui-readiness.mjs`. Browser proof creates a dedicated preview and owns its ports. It writes generated sample and evaluation output; preserve existing operator data before using these commands in a working project. The retained matched scripts document the original operator replay; their historical absolute paths are provenance, not portable launcher paths.

Use [manifest.json](manifest.json) to verify each copied artifact. Manifest keys are paths relative to this directory. Raw historical reports retain their original path values and are copied without rewriting; raw Markdown is `.md.txt` so citation checking does not treat old claims as current documentation. The final independent staged-tree judgment is held outside this self-hashed packet to avoid circular claims.

Full visual, design, responsive, interaction, accessibility, performance, usage and alignment grades remain null. These are bounded anonymous Chromium jobs with emulated widths; physical touch, other engines, arbitrary graph sizes, a human usability session and deployment are unrun. Earlier performance results cannot grade the changed renderer. Source and installed demo proof do not certify imported assertions or agent authority.
