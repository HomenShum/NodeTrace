# NodeTrace entity inspection repair

**Builder. Re your request:** make each repo usable for developer/user handoff, with visual, responsive and interaction proof. This feature repair closes the confirmed keyboard entity-inspection failure and the graph's clipped labels. Whole-product readiness remains separate.

Named proof: **NODETRACE-ENTITY-INSPECTION-02**. A keyboard or pointer user can choose every actual snapshot entity, read its complete kind/name and exact producing trace-event IDs, and use Fit at all seven declared widths in source and an actual installed consumer. Every painted label stays inside its canvas without colliding with another painted label; full identities remain available in the named DOM control/readout.

## Cause and frozen approach

The current pointer callback is the only route to the producing-event readout. Canvas nodes have no semantic keyboard control. The renderer's default text painter writes each label to the right of its node without checking measured text extents or other labels. A coarse label grid does not prevent long names colliding or crossing the canvas border. Existing modal/tab checks and document-overflow checks did not exercise this advertised entity job or canvas glyph bounds.

1. Add one native labeled entity selector in `src/trace/LiveGraphRail.tsx`, deriving options and selected identity from current snapshot nodes. Pointer callback and native selection use the same existing event mapping. Store the selected ID and derive current data so a removed entity cannot retain stale event details. The result wraps long names/IDs and remains readable through natural keyboard focus.
2. Use Sigma's existing `defaultDrawNodeLabel` and `defaultDrawNodeHover` seams inside the vendored `NodeGraph.js`. Measure actual font extents, try adjacent placements inside the canvas and avoid occupied label rectangles in the current frame. Bound frame bookkeeping to actual painted nodes and reset it at render. Long/dense labels may shorten or be omitted only with explicit visible guidance that the named selector provides all complete identities. Do not change graph topology, positions, counts, edge meanings, visit semantics or data authority.
3. Apply the existing44px control preference and visible focus in the owned graph CSS. Keep the existing Fit action and filters. No new renderer props or external dependency are needed for the UI feature.
4. Extend the existing source-bound browser proof with the actual keyboard/pointer job, all fixture entities, long names, repeated selection, current-frame label bounds, Fit/pan and stale/empty state. Preserve every existing assertion. Capture DOM, console and pixels before resizing. Run source/installed journeys, affected package checks and a fresh judge.

## Technical prerequisite and boundary

The primary checkout's source is clean at c578e3d but its old node_modules lacks four declared graph packages. Its inherited browserslist4.28.2 development dependency has two audit findings. First preserve the current source/index/refs and lock, update only the compatible browserslist lock closure, then run normal `npm ci`, audit and build. No global installation or package manifest expansion.

This prerequisite changes a proof-bound lockfile. Before the first UI edit, capture a fresh actual primary source boundary against the synchronized dependency tree, retaining its actual six-event state, plus the unchanged installed four-event reference. The earlier preflight remains valid for its explicitly recorded four-event retained-runtime cells. Do not silently relabel those cells as the primary installation.

Root has viewed the earlier actual390px populated and pointer-selected3px boundaries. CHANGE A covers entity controls and their necessary flow-height increase. CHANGE B covers the canvas and existing event result. Protect all headers/setup commands, installed guidance, native modal, source-opening policy, tabs/history, unrelated query/hash, fixtures and authority. Any changed input outside this contract requires an explicit addendum before edits.

Allowed implementation paths: `src/trace/LiveGraphRail.tsx`, `src/styles.css`, `vendor/nodegraph-live/NodeGraph.js`, `scripts/ui-readiness.mjs`; technical prerequisite `package-lock.json`; new evidence and concise HANDOFF/provenance and exact citation updates if source lines move. Vendor provenance must disclose the local label-only delta. Normalize touched files before final evidence binding.

Limits: full mobile evidence-density work, arbitrary production graph scale, other browser engines, physical input, actual human usability and production are not certified by this slice. Existing WIG red evidence and its probe-assumption disposition remain unchanged. Current-frame canvas measurements cannot reuse accumulated label draws.

## Pre-edit placement addendum — 2026-09-05

Primary dependency synchronization is complete: normal npm ci, full audit with zero findings, and build pass. The fresh primary-synchronized replay captures the actual six-event source (14 entities,31 edges) and unchanged four-event installed consumer; all48 diagnostic checks confirm the keyboard failure and all input/ref/status guards pass.

The selector will live in LiveGraphRail immediately before NodeGraph. This uses the existing public callback without adding a vendor API or DOM portal. Its insertion necessarily moves the local Relationship graph heading downward. Before UI edits, the primary-controls-boundary replay expands CHANGE A precisely from that local graph heading through the existing controls row; its after footprint includes the selector and that intentional downward flow. The local heading text/counts remain unchanged. The surrounding Live graph rail header and all page/guide content remain protected. CHANGE B retains canvas and existing result. This is a declared placement adjustment before implementation, not permission for neighboring redesign.

The current-snapshot selector exposes a related stale-pointer readout seam: the vendored count/visits panel dereferences its earlier selected ID after removal. Its display also requires the ID in current input nodes and the patched graph. This belongs to the named removed-entity proof, without a new prop or pointer callback change. Vendor provenance records this guard and the label painter. The actual primary-controls-boundary390 screenshot was viewed before UI edits; six cells/48 diagnosis and preservation checks passed. A first packet-note append used the app working directory and failed after source normalization; no app edit was lost and the subsequent build passed. This entry corrects the note path.

Verification uses one shared scripts/lib/entity-inspection-proof.mjs helper from the existing source journey and the fresh installed consumer replay. This additional proof-only path prevents divergent keyboard/canvas checks between the two actual consumers. It instruments current real glyph draws, requires positive paint, invalidates records on clear, and checks actual named controls and input event IDs. It is not shipped application behavior.

## Independent-review correction — one current selection and snapshot

The first complete source replay passed1830 checks/86 captures, but an independent component host reproduced a missing transition: pointer selection then native selection left the vendor detail panel on the prior entity. It also reproduced stale visits after same-entity event growth and permanent dimming after the hovered entity was removed. Root viewed the mismatch screenshot; all failing inputs and pixels are retained separately. Those failures refute the first candidate's complete entity-inspection claim.

Correct the selection seam once: the local vendor gains one optional controlled selectedNodeId prop (NodeGraph.d.ts is added to the authored scope), supplied by LiveGraphRail. Derive detail messages, visits and events from current props and the same selected ID, using one message builder for pointer callbacks and readout. Existing consumers that omit the prop retain internal selection. In this controlled rail the explicit selector owns selection clearing; blank canvas clicks do not silently contradict its selected value. Clear renderer hover state when snapshot inputs change so removed hover identities cannot dim unrelated current entities. This replaces the earlier no-new-prop assumption because actual observed correctness requires one owner.

The shared proof adds pointer→native→clear transitions, exact current visits, overflow checks for each selected full identity, and screenshots while long identities are selected. The earlier passing matrix stays retained, not relabeled as final evidence for corrected bytes. Other graph meanings, layout settings and before-boundary protections remain unchanged.

## Fresh Vite consumer prerequisite

Normal npm run check passes, including a retained actual Next consumer build. The separate ordinary Vite React/strict-TypeScript host fails its unchanged build because the generated demo entry passes HTMLElement|null to createRoot. The installer owns that entry's matching root markup, so its generated lookup can use a nonnull assertion. Add this one Vite-only generator correction in bin/nodetrace.mjs to the current required installed-consumer proof. Preserve the raw failed setup receipt/log and exact generator before bytes. This does not change source/Next rendering input. After compiling, verify the actual production demo route rather than accepting the host's build exit alone.

Vite route adjudication: read E6c_NODETRACE_VITE_BUILD_DISPOSITION.md. The host production build omits the optional demo HTML, but the existing generated contract explicitly promises the dev route. Keep host config/entry untouched; prove that actual dev route and strict host build separately. No new production-demo build abstraction or production claim is added. Preserve the provisional route P1 probe and its correction. The genuine compile defect remains repaired by the one generated nonnull assertion.
# Final compatibility correction: one canonical graph owner

A developer rendering their own typed graph can supply reversed duplicate edges or an endpoint absent from the node list. The canvas model already reconciles those inputs. The new raw-input nodeMessage bypassed that owner and displayed additional assertion badges and callback edges absent from the canvas. The actual mounted baseline retains one post-patch edge at weight99; the candidate displayed two at weights1 and99. Preserve the actual patched graph's policy, including its existing duplicate resolution; do not introduce a second normalization or alter graph-model.js.

Root viewed the genuine900x1100 direct-renderer baseline and candidate 3px CHANGE B images in E6c-nodetrace-node-message-browser-replay before this correction. The selected detail/assertion region is the only visible delta. Their raw callbacks and source hashes are retained. The earlier165-check matched6/4-fixture proof and two65-check corrected captures remain prior-candidate evidence; no old source binding will be promoted to final.

Correct only NodeGraph.js: nodeMessage reads the actual graph for both detail and pointer callback; patch before paint and increment the existing React revision after each input patch so attribute-only updates are visible; reset filters only when the actual edge-type signature changes. Keep direct visits props, optional controlled selection, current-frame label painter, positions, runtime/edge schemas and graph-model normalization unchanged. No extra graph instance, dependency or public API is added.

Required proof: direct duplicate/dangling assertions versus the actual canvas; attribute-only weights/counts/visits on a selected entity; filter choices survive metadata-only changes; current pointer/native/clear/removal transitions; final source matrix and updated installed renderer checks. Preserve all earlier passing and failing packets. Final portable staging waits for these final bytes and independent review.

Canonical-owner addendum: the independent f19fbeff replay confirms omitted optional visits retain7 in the existing model/baseline but the candidate displays0. Root viewed the actual selected detail before correction. Restore the readout from the actual graph now that before-paint patch/revision guarantees freshness. This supersedes the earlier direct-visits-props instruction and preserves graph-model policy. No field, normalization, layout or public API changes.
