Builder. **The application recovers correctly; the failure was in the proof.** A developer sees the actual HTTP503 message and a button named **Retry loading trace**. The proof waited for the exact name **Retry**, which does not match that button.

**Re your request:** independently diagnose the final Raw proof Retry failure without source edits or weaker error recovery. The actual error and successful retry are now observed in DOM, console, request records and pixels.

The original full run remains `ok:false` with308checks/23captures. Its degraded page was also missing `activePage` ownership and console/pageerror listeners, so failure capture looked at the already closed last responsive page. That explains the absent Retry failure screenshot; it does not erase the recorded responsive work or turn the run green. [Original failed report](E6d-nodetrace-raw-after-final/report.json)

The independently frozen replay passes11checks: the first request receives503 and renders the visible alert; the old exact Retry locator matches zero controls; **Retry loading trace** is enabled; clicking it causes exactly one second request with200 and the exact fixture bytes; Raw shows the semantically identical complete selected step and the exact paired before-JSON hash; the error and Retry control disappear. Served assets match the current build. Source, build index, public fixture, HEAD and index entries remain unchanged. [Raw replay](E6d-nodetrace-raw-retry-judge-03/report.json), [actual failure pixels](E6d-nodetrace-raw-retry-judge-03/actual-error-before-retry-viewport.png), [actual recovered pixels](E6d-nodetrace-raw-retry-judge-03/actual-recovered-raw-viewport.png)

The browser records one expected HTTP503 resource error, bound to the intentionally failed state-request URL, and no page crash or other console error. The current frozen proof script8a89a135 uses the correct exact name, owns the actual degraded page, records its console/errors, asserts a visible error before Retry, and narrowly distinguishes that exact expected503 message. These are proof corrections; the product loader and button need no change.

Two independent attempts remain retained. The first reached recovery but used an order-sensitive JSON.stringify comparison; selected-step objects were semantically identical with reordered keys. The second reached recovered screenshots, but its final custody check correctly refused passing output when root edited the proof script concurrently. Root confirmed that edit and froze the script before the final11-check replay. Neither earlier attempt is claimed as passing.

Finish the planned full source and installed-consumer runs with the frozen proof, then judge their final bindings. This bounded diagnosis adds no product grade, deployment or production-outage claim. [Complete judgment and evidence hashes](E6d_NODETRACE_RAW_RETRY_CAUSAL_JUDGE.json)
