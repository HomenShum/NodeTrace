Builder. **The bounded Raw reading repair passes its source and normally installed consumer journeys; independent final review is next.**

**Re your request:** make each repository usable and handoff-ready with visual, responsive and interaction grades. A developer comparing JSON with its graph can now focus and scroll the full named Raw region without the entire payload displacing the next section. The change touches the existing pre and its CSS only; the proof script is separate. Full JSON output, colors, graph meanings and native browser keys remain unchanged.

Source proof passes 312 checks/25 captures over eleven cells, including actual/short/long inputs, component200% text, reduced motion, keyboard/pointer, entity/step identity, reload/history, bursts,60-second reading and503/Retry. The representative ordinary Next install/build passes, then four short/long390/1024 replay cells pass 104 checks/8 captures. All 53 recorded consumer source/state/build inputs and exact served assets remain unchanged. The installed replay explicitly supplies the recorded coach JSON and19source-bound capture attachments; it does not claim the normal four-event consumer generated them.

The source run uses its retained proof-script bytes; the later consumer-only attachment/custody extension changes no app bytes. Earlier Home timing, Retry locator and incomplete-fixture failures remain available with independent causal reports. Native Home issued before an earlier scroll finishes remains a Chromium limitation; completed-action proof waits the actual scrollend and still requires exact0. No application key handler or broad404 filter was added.

The source plan and real labelled before boundary remain binding. D4/R3 repaired observations require independent judgment; D3 mobile setup prominence remains outside this patch. No full grade, human/device test, production performance, commit or shared release is claimed. Read E6d_NODETRACE_RAW_IMPLEMENTATION_RECEIPT.json for exact current sources and evidence references.
