Builder. **Correct the proof sequencing; no product key handler is warranted.** A developer reading a long Raw trace can return to its first line with native Home once the preceding native End scroll completes. The proof sometimes issued Home before that completion and treated an early endpoint sample as a completed action.

**Re your request:** independently diagnose the NodeTrace Raw keyboard failure without source edits or a weaker Home requirement. The failure is reproduced, isolated from the application, and resolved by an event-bound proof sequence.

In the instrumented actual app run, End first reaches `scrollTop=max=11345` at1807.5ms. Home arrives1811.9ms; the preceding native `scrollend` arrives1824.8ms. Home is trusted and not prevented, yet the position remains11345. The same connected pre retains identity3, focus, geometry, document position and an empty selection. A second native Home later reaches0 on that same element. [Actual failure events/frames](E6d-nodetrace-raw-home-judge-01/native-home-failure.json), [failure pixels](E6d-nodetrace-raw-home-judge-01/native-home-failure.png), [separate second-key recovery](E6d-nodetrace-raw-home-judge-01/native-home-second-key.json)

The decisive control uses the exact captured application DOM and current built CSS with every application script removed. It retains the same332×548.59 reading region, max11345 and exact long JSON. Sampling the endpoint then immediately sending Home fails9 of10 Home actions. Waiting for actual native `scrollend` passes20 of20 End/Home actions. React, history restoration, application handlers and graph animation are therefore not necessary for the failure. This establishes the browser-native sequencing boundary; it does not identify Chromium's internal implementation defect. [Static control raw evidence](E6d_RAW_HOME_STATIC_JUDGE.json)

The proof-only completion derivative then passes the original actual-app flow: **72 checks, 3 captured states, 44 full End/Home reading rounds across60.812seconds**. It preserves actual native key input, full JSON hash, focus, selection, tab/record transitions, reload, Back/Forward and strict `Home => scrollTop === 0`. Source, public fixture and served build bindings are unchanged. [Actual completed reading report](E6d-nodetrace-raw-home-completed-01/report.json), [native events](E6d-nodetrace-raw-home-completed-01/native-completed-reading.json), [first-line pixels after sustained reading](E6d-nodetrace-raw-home-completed-01/long-390-no-preference-sustained-viewport.png)

The smallest correction is one proof helper in `scripts/raw-reading-proof.mjs`: focus the region; attach an element-specific `scrollend` listener before the key; press the real native key; await completion with a bounded timeout and the unchanged endpoint assertion; remove the listener on completion/failure. If already at the requested endpoint, keep the assertion without waiting for a movement event that may never occur. No product handler, programmatic scroll assignment, hidden key retry, disabled motion or fixed-delay substitute is needed.

Keep the original failed run and all diagnostic failures. The independent failure capture `native-home-failure` records the stuck state; `native-home-second-key` records explicit recovery. The derivative's inherited `failure-current-state` is captured after that recovery and is not the initial failure image. This distinction is preserved in the machine report.

This completes the bounded causal diagnosis only. Rapid interruption before native completion remains a recorded Chromium149.0.7827.55 behavior. The remaining planned source/consumer cells and final Raw judgment still need completion; no whole-product, responsiveness, interaction, physical-device, performance or human grade is increased here. No product source, build, index or refs were changed by this judge. [Complete judgment and evidence hashes](E6d_NODETRACE_RAW_HOME_CAUSAL_JUDGE.json)
