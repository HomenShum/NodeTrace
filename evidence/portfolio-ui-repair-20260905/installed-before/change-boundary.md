# Installed recovery guidance before-state

Actual retained Next production build. Route: http://127.0.0.1:39743/nodetrace?host=preserved&returnTo=dashboard#host-section. Viewport: 390 × 844 CSS pixels at device scale 1. Screenshot mode: full page; existing light theme; fresh anonymous Chromium. Real installed SQLite sample; lens closed; no Trace Coach records.

- CHANGE A / setup command region: .showcaseActions, covering the three source-repository commands and their no-key badge. Adapt commands to the installed host context.
- CHANGE B / empty-coach instructions: .coachEmpty, covering its heading and recovery paragraph. Adapt the recommended next step to what the installed consumer can actually run.

Preserve the hero, current-state summary, Inspect control/modal, status summary, and live graph behavior. Exact observed document-coordinate bounds and source/build/state SHA-256 bindings are in boundary-contract.json.

The clean screenshot was captured first. The annotated screenshot uses 3px DOM outlines and legible labels on the same actual page; no image reconstruction or source edits. Source unchanged: true. Browser was closed after capture.
