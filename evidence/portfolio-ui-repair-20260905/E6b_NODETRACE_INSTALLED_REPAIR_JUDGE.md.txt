VERIFIED WITH CAVEATS — the installed recovery-command failure is resolved on the exact candidate below. No new in-scope blocker was observed. This is bounded repair acceptance, not full product readiness.

**Re your request:** verify the narrow installed-context repair in a fresh consumer, execute the displayed command, inspect mobile/desktop before and after, rerun the source-bound journey and unchanged WIG audit, and preserve the earlier refutation. Builder; read-only source review. All original application files, refs and historical judge receipts remain unchanged by this judge.

A developer installing NodeTrace now sees a command that exists in the installed project. Running it regenerates the sample trace. The page explicitly explains that guided source/UI captures are not included and points to an integration guide that exists in that project. The source checkout retains its own capture commands.

| Claim | Actual independent result |
| --- | --- |
| Smallest correct seam | Only `src/DemoDashboard.tsx` and `bin/nodetrace.mjs` differ from the prior judge snapshot. The optional demo-only `installed` prop defaults false; generated Next and Vite entrypoints supply it explicitly. Provider API, public authority, URL parsing, styles, modal and loader bytes are unchanged. |
| Real fresh installation | PASS. Unchanged installer E2E performed dependency installation, real SQLite generation, target smoke and Next 15.5.25 production build in 97.977 seconds. Retained target: `D:/ProjectRecovery/nodetrace-installed-repair-consumer-bWuHqU/temp/nodetrace-next-e2e-aK4D95`. No skip-install/skip-verify was used for this Next proof. |
| Advertised recovery command works | PASS twice, obtained from the rendered page at 390 and 1440. `npm run nodetrace:happy-path` exited 0 each time, changed the state SHA-256 and regenerated four trace records and two proof records. Refresh showed useful data with the host URL intact. The old missing command is absent from both installed screenshots. |
| Installed interaction and rendering | PASS: 52 checks, six captured states, 44 exact-byte served state/build-asset bindings. At 390×844 and 1440×960, natural Tab/Enter opens the native modal, focus remains inside, Escape and pointer close return focus, and pathname/unrelated query/hash survive. No uncaught script error or document horizontal overflow occurred. |
| Source-default behavior survives | PASS: the unchanged named journey ran on all 13 new proof-source hashes, yielding 808 passing checks and 43 captures across seven widths. Actual source-demo HTML retains all three source commands and its original empty-state guidance at every width. Real error/timeout/retry/history/late-response cases remain in this run. The sustained-loop assertion count varies with elapsed time. |
| Compilation, smoke and citations | PASS. Source journey builds include TypeScript. Separate SQLite/API/CLI/MCP smoke passes; citation check passes three tours, 35 steps and 54 Markdown citations. A first citation failure was a judge-clone assembly omission, documented below. |
| Existing WIG gate | FAIL retained unchanged: two major findings. Raw measurements show selected Raw before/after with `survivesReload:true`, and valid empty state with `heroClaimsSeededFromRealFiles:false` and `pointerToTheMissingCommand:true`. These do not demonstrate a current navigation/empty-state defect; the probe clicks the already-selected tab and requires a populated coach. No assertion, floor, or report was rewritten to PASS. |
| Quiet web-quality measurement | Root's actual retained run at `2026-09-05T01:54:36.135Z` passes: TBT 320/234ms, LCP 1917.447/1874.105ms, CLS 0.018724/0.037447, performance 0.92/0.95 and zero axe violations in both seeds. Earlier 842/900.5ms failures remain preserved. This is a measured local run with the provenance limitation below, not a field-performance grade. |

BOUNDARY PROOF: The genuine pre-edit installed 390×844 screenshot and labeled 3px CHANGE A setup / CHANGE B empty-state boxes remain at `evidence/nodetrace-installed-recovery-boundary/`. I compared them with actual new clean and annotated 390 pixels, plus new desktop empty/modal pixels. Header commands and the empty instructions change inside those regions; the shorter setup naturally moves subsequent sections upward. Headline, summary, Inspect control, status content and renderer implementation remain consistent. For a same-size desktop comparison, the untouched retained prior build was also replayed at 1440×960. That desktop reference is explicitly labeled a retrospective prior-build replay, not a pre-edit boundary capture. Graph force-layout pixels vary and labels still overlap/clip; no graph-layout or whole-page pixel-equivalence claim is made.

CAUSE: The old installed page inherited source-repository instructions without knowing its execution context. The installer supplied `nodetrace:happy-path` while the page advertised `trace-coach:sqlite`, so the user hit `Missing script` despite a successful build. The prior installer test never executed the command actually shown by the page.

FIX: The root's explicit installed flag reaches only the demo UI through generated framework entrypoints. Installed copy uses the existing consumer command and actual local guide, while disclosing missing guided captures. It introduces no library provider API, dependency, URL flag or fabricated script alias. All three installed helper files match the source with only the expected import rewrite. Actual Vite generation includes `<DemoDashboard installed />`; that additional Vite check is generation-only and does not claim a separate installed Vite production build.

KNOCKOUT: RESOLVED. Raw command logs are `E6b-nodetrace-installed-repair-browser/390-advertised-command.log` and `1440-advertised-command.log`; before/after state digests and record counts are in `E6b-nodetrace-installed-repair-browser.json`. The old `E6b_NODETRACE_UI_JUDGE.md/json` refutation and failing command remain byte-identical. This receipt supersedes only that installed-command finding.

LIVE PROOF: Fresh anonymous local Chromium production previews only. The installed route preserves `/nodetrace?host=preserved&returnTo=dashboard#host-section`. No deployment, provider call, authenticated session, physical device, human participant or public production claim is included. Preview/browser processes are stopped.

RESIDUAL RISK: Full portfolio readiness remains OPEN and all eight final dimension scores plus overall grade remain null. Physical touch, screen readers, human first-run comprehension, other engines, 200% text, reduced motion and full constrained-device performance are not established here. Inherited graph-label clipping and development-dependency advisory remain outside this copy repair. The source WIG report is still red and must not be described as a passing gate.

Audit provenance: Root did not capture an explicit 13-file source hash pair around the quiet audit. Its actual built-tree/state guard and network-log asset names are retained. Both raw Lighthouse files name `index-Cl47s1zn.js` and `index-C5GRn8kk.css`; current root and independently built candidate assets have identical bytes. This supports continuity as an inference. The separately observed frozen source identity must not be retroactively presented as an audit-time snapshot. Machine contention explains neither prior failure conclusively nor all future performance.

Judge-run correction: The isolated clone initially omitted four changed `promotion/*.md` files, retaining base citations. The first citation log is preserved. Copying their exact frozen root bytes into the judge clone, without editing the root or relaxing checks, produced the 54-citation PASS. `E6b-nodetrace-installed-repair-clone-docs-adjudication.json` records the cause and hashes. Final identity covers those four additional documents: 53 source/document/package inputs match, all 13 proof hashes match the fresh journey, and HEAD/refs and historical receipts remain unchanged.

Primary evidence: `E6b-nodetrace-installed-repair-final-identity.json`, `E6b-nodetrace-installed-repair-narrow.diff`, `E6b-nodetrace-installed-repair-consumer-setup.log`, `E6b-nodetrace-installed-repair-browser.json`, `E6b-nodetrace-installed-repair-journey/receipt.json`, `E6b-nodetrace-installed-repair-wig-artifacts/wig-review.json`, and `E6b-nodetrace-installed-repair-quiet-audits/`. Final raw hashes and limitations are also in the companion JSON.
