REFUTED — the installed empty-state recovery command fails. The independently replayed source-demo interaction repairs and installed modal runtime pass; this verdict applies to the frozen candidate identified below, before the subsequent installed-copy repair.

**Re your request:** independently judge the NodeTrace repair using exact source, real keyboard/pointer/history/error behavior, rendered pixels, and a fresh installed consumer. Builder; read-only review. The original portfolio readiness request remains OPEN.

A developer installing the trace viewer can now open and dismiss its evidence using an ordinary button and keyboard. However, when no guided records exist, the installed viewer tells that developer to run a command absent from the installed package. Compilation and a working modal do not complete that recovery job.

| Claim | Independent observation and verdict |
| --- | --- |
| Proof runs the frozen candidate | VERIFIED. All 49 frozen implementation/document/package inputs and HEAD/refs remained unchanged. Thirteen proof-source hashes match the root's final 686-check/43-capture receipt and the judge's fresh 677-check/43-capture run. The sustained-loop check count varies with time; all assertions pass in each retained run. |
| Normal entry, native modality and focus return | VERIFIED in the exercised Chromium cells. Natural Tab/Enter opens the ordinary inspector; unknown registry entries disclose absence. Native modal focuses Close, keeps endpoint Tab/Shift+Tab inside, and returns usable focus after Escape/close. A drag begun inside does not count as a backdrop click. Independent focus screenshots show the final 3px ring/3px offset and 12px clearance above the tab row at 390 and 1440. |
| Tabs, URL and history retain the selected record | VERIFIED. Independent Overview-to-Raw selection changes the URL; clicking already-selected Raw is correctly idempotent. Three Step A/Raw → Step B/Overview → Back → Forward cycles restore both fields. A held real state response followed by navigation and Back hydrates the latest historical selection when released. Unrelated query and fragment survive. |
| Bad or late public data stays honest and bounded | VERIFIED for the named cases. HTTP 503, malformed JSON, missing nested rectangle fields, unsafe URL, oversized body, ten-second timeout and Retry are exercised. Late responses do not overwrite current state. Empty source sentinel and captureKind are retained. Valid `constructor`/`__proto__` grouping labels do not crash. Public JSON and query flags cannot grant builder authority. |
| Rendered result matches the source and assets | VERIFIED. The separate independent replay has 191 passing checks/18 captured states, 42 exact-byte served response bindings, decoded source/UI images, and no uncaught page errors. Both seed runs share the same 18 captured-asset hashes with the root receipt. The independently installed Next consumer binds its state and all ten served JS/CSS assets to its retained production build. |
| Fresh consumer installs, builds and exposes the trace | VERIFIED for the named Next consumer. Unchanged installer E2E performed actual install, SQLite generation, smoke and Next 15.5.25 production build. Forty-six imports across eighteen modules resolve, including both new demo helpers and existing-owned Zod. Nine runtime checks verify hydration, keyboard Inspect, real Status proof/trace, Escape/focus return and preserved host pathname/query/hash. |
| Empty-state instructions give an actual next command in the installed consumer | REFUTED, P2. At `/nodetrace`, the UI says `npm run trace-coach:sqlite`; that exact command exits 1 with `Missing script: "trace-coach:sqlite"`. The installed package only provides the consumer-prefixed setup/smoke scripts. |
| Web quality and full portfolio readiness pass | NOT VERIFIED. The retained final pre-quiet Lighthouse run fails the 600ms TBT gate at 842/900.5ms. A later quiet audit is owned by root and is outside this receipt. No full grade follows from these bounded agent checks. |

BOUNDARY PROOF: The actual pre-edit source-demo captures and 3px annotated CHANGE A/B/C/D regions are preserved in `evidence/nodetrace-user-journey-20260904/`. I inspected the before 1440 boundary and lens images, the first-patch mobile modal, every final overview and modal at 320, 360, 390, 768, 1024, 1440 and 1920, the final focused detail controls at 390/1440, and actual happy-empty, HTTP-error and pending-data pixels. The final badge stays whole at 320 and the primary actions remain reachable. Neighboring capture assets are byte-identical. Force-layout graph pixels are not deterministic; graph labels still overlap or clip at narrow widths. This is an explicit broader responsive deduction, not a claim that unchanged graph sizing is solved. Before captures are real historical interfaces; no reconstructed before image or whole-page pixel-equivalence claim is used. The installed-command defect has a separate actual 390x844 clean and annotated before capture in `evidence/nodetrace-installed-recovery-boundary/`; its 3px CHANGE A setup and CHANGE B empty-state boxes, exact route/query/fragment, DOM/AX and unchanged source/consumer hashes were independently inspected before releasing the copy-path freeze.

CAUSE: The first failed boundary is source-demo guidance copied into a generic host. `src/DemoDashboard.tsx:82` renders source-repository commands and line 135 uses one as the empty-state recovery action. `bin/nodetrace.mjs:169` installs different, consumer-prefixed scripts; generated Vite/Next entrypoints at lines 319/332 render the same demo without execution-context information. The installer test validates installation, data generation, smoke and compilation, but never executes the recovery command shown by the installed page. The changeable root cause is the absence of explicit installed-context guidance in the transplanted demo.

FIX: Give the demo entrypoint explicit installed-context information from generated entrypoints. Render the supported `npm run nodetrace:happy-path` and the installed integration guide, and explain that guided captures are not included. Preserve source-checkout commands in the source demo. Do not add a misleading alias for an unavailable bundled capture workflow or derive context/authority from a URL. This judge made no application edits.

KNOCKOUT: UNRESOLVED at this frozen revision. Raw failed command is `E6b-nodetrace-consumer-recovery-command.log`. A subsequent repair must install afresh, inspect the actual empty state, run every advertised recovery command in that consumer, verify useful regenerated data, and reopen the same route. Preserve this refutation when producing a new receipt.

LIVE PROOF: All runtime observations are anonymous local production-build previews and a fresh installed Next consumer. No production deployment, provider request, authenticated session, real physical device or human participant was exercised. Browser and preview processes were stopped after their evidence sets.

RESIDUAL RISK: The pre-quiet Lighthouse measurement fails despite a previous 541/537ms observation; machine contention is only a hypothesis until the quiet measurement exists. Both audited seeds have zero axe violations but retain incomplete checks. The independent eighteen-state axe scan also has zero violations; this is not screen-reader certification. Normal `npm ci` reports one inherited high development dependency (`browserslist@4.28.2`), already present in base fc7's lock; root's audit zero is production-only. Graph label clipping, dense mobile nested scrolling, small source-capture bitmap text, 200% text, reduced motion, other engines, physical touch/keyboard and human comprehension remain outside completed coverage. All eight final dimension scores and overall grade remain null.

Evidence: `E6b-nodetrace-judge-final-identity.json`, `E6b-nodetrace-judge-before.json`, `E6b-nodetrace-judge-source.diff`, `E6b-nodetrace-judge-journey/receipt.json`, `E6b-nodetrace-judge-browser.json`, `E6b-nodetrace-judge-focus.json`, `E6b_NODETRACE_CONSUMER_JUDGE.json`, `E6b_NODETRACE_CONSUMER_RUNTIME_SUPPLEMENT.json`, `E6b-nodetrace-judge-retained-audits-before-quiet/`, and `E6b-nodetrace-judge-audit.json`. The original WIG and web-quality probes are assertion-identical to base after line-ending normalization; no threshold or assertion was weakened. The original WIG failures and first-repair report remain historical. The final root WIG run was not executed after its audit chain stopped; independent scenarios adjudicate its selected-Raw and empty-coach assumptions without relabeling that report PASS.
