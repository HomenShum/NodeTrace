# NodeTrace developer handoff

A developer can inspect saved trace events, source captures and proof records in the source demo, or install the portable trace UI in an existing React application. NodeTrace does not run an agent or certify the claims shown in imported records.

## Run and verify

Use Node 22 and `npm ci`, then `npm run happy-path` and `npm run dev` for the local SQLite sample. `npm run trace-coach:sqlite` prepares the bundled guided snapshot. The source-repository commands for new captures require a real NodeRoom checkout; the snapshot does not imply that checkout exists.

Run `npm run check` for the repository package, capture and installed Next checks. Run `node scripts/ui-readiness.mjs` for the rendered state/history/keyboard/recovery journey at seven widths. It creates its own preview, exercises both seed states and stores timestamped evidence. Do not run a second build concurrently. The source state request is bounded and has explicit timeout/retry; public producers remain responsible for excluding privileged records before publishing their JSON.

For a separate application, follow README and the CLI-generated `docs/NODETRACE_INTEGRATION.md`. The installed demo explicitly selects installed guidance: `npm run nodetrace:happy-path` regenerates its local sample. Guided source/UI captures are not included in that generated sample. The installer preserves existing dependency ranges. A fresh Next consumer was installed, built and exercised. A fresh strict Vite/React host now installs and builds successfully; its generated root matches the installer-owned HTML. Open the Vite demo at `/nodetrace.html` in the dev server, as the generated guide says. Including TraceLens in a production host uses that host's existing component/build entry; the optional demo HTML is not automatically part of its production build.

## Reviewed changes and proof

The earlier repair added ordinary inspection controls, a native keyboard modal with reliable focus return, demo-owned deep links/history, connected keyboard tabs, readable ink, bounded state validation and recovery, and truthful source/installed guidance. Builder authority is not read from URL parameters or public state.

The current entity repair lets a reviewer reach every current actor, tool, artifact and step through the native Entity selector, then read its complete identity and producing-event IDs. Pointer selection and keyboard selection share one owner. Both detail panels, visits and hover styling follow the same reconciled graph as the canvas. Metadata refresh preserves unchanged type filters, retained optional visits, and the existing duplicate/dangling-edge behavior. The local graph renderer measures current glyphs to place, shorten or hide crowded labels; the selector retains complete names. Graph positions, event meanings, edge types and measured-count semantics are unchanged. The compatible development-tool lock update resolves the two observed browserslist advisories; the recorded full audit has zero findings.

Current proof is in `evidence/entity-inspection-20260905/README.md`. The final source journey has 3368 checks/128 captures at seven widths and 16 implementation bindings. Independent built Next proof has 1855 checks/95 captures; independent Vite development proof has 1874 checks/95 captures. Both installed apps executed their displayed refresh command and rendered the new event IDs. These final owner proofs follow a disclosed one-file renderer update and ordinary rebuild of the normally installed consumers; they do not claim a second full installation. The separate independent transition knockout has 127 checks, including the three real failures found in the first candidate. Check the exact source and installed bindings before reusing these observations. The matched comparison reuses the original six-event source and four-event installed inputs at 320, 390 and 1440 pixels: six completed cells, 12 captures and 36 exact saved PNGs pass the raster check. It combines five completed cells with one successful cell retry; the failed full run, probe encoding failure and retry remain available. Counts vary with sustained-session iterations; they are not product scores.

Historical proof remains unchanged in `evidence/portfolio-ui-repair-20260905/README.md`: 808 source and 52 installed checks, quiet lab accessibility/performance, the earlier slower audit and adjudicated WIG failures. Those earlier lab results do not certify the new graph renderer's performance.

## Remaining readiness work

The bounded entity repair is independently approved at commit `6048117a7879bde8b7a214746a4e4e49607bfe5f`; see [the final judgment](evidence/entity-inspection-final-review-20260905/README.md). Shared integration is pending normal CI. The recorded entity jobs close the observed keyboard, label, stale-selection, visit-count and removed-hover failures; they do not prove arbitrary graph sizes or consumer data. Full visual/design/responsive/interaction/accessibility/performance/usage/alignment grades, physical touch, a fresh human usability session and deployment remain pending. Source and installed demo proof does not certify an agent's claims or a production system.
